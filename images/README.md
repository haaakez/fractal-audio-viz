# README images

This directory contains the lightweight visual assets linked from the project
README. `preview.GIF` is intentionally a small placeholder until a polished
render is selected; `gui.png` is a screenshot of the GTK launcher.
